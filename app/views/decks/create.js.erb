function refreshForm(innerHTML) {
  var newDeckForm = document.querySelector("#modal-deck-creation");
  console.log("je suis ici");
  console.log(newDeckForm);
  newDeckForm.innerHTML = innerHTML;
}

function adddeck(deckHTML) {
  const decks = document.querySelector("#new-deck")
  decks.insertAdjacentHTML('beforeend', deckHTML);
}

<% if @deck.errors.any? %>
  refreshForm('<%= j render "decks/form_modal", theme: @theme, deck: @deck %>');
<% else %>
  // adddeck('<%#= j render "decks/show", deck: @deck %>');
  window.location = '<%= deck_path(@deck) %>'
<% end %>
